services:
  - type: web
    name: myschoolgn
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash scripts/render_start.sh
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: ecole_moderne.settings
      - key: PYTHON_VERSION
        value: 3.12.8
